:root{
  --TitleFont : {{ settings.title__font }};
  --BodyFont : {{ settings.body__font }} ;
}
    
 body{
   font-family:var(--BodyFont);
    font-weight: 400;
    font-style: normal;
    font-size: 18px;
    text-transform: none;
    color: #333;
    line-height: 1.6em;
    overflow: auto;
 }